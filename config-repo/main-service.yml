spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL_MAIN_SERVICE}
    username: ${SPRING_DATASOURCE_USERNAME_MAIN_SERVICE}
    password: ${SPRING_DATASOURCE_PASSWORD_MAIN_SERVICE}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: false
server:
  port: 0